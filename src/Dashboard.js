import React from 'react';
import { Container, Typography } from '@mui/material';

const Dashboard = () => {
  return (
    <Container>
      <Typography variant="h4">Добро пожаловать на вашу панель управления!</Typography>
      {/* Добавьте здесь контент для вашей панели управления */}
    </Container>
  );
};

export default Dashboard;
